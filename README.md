# colourpicker
A sleek, real-time CSS Gradient Generator built with HTML, Tailwind CSS, and JavaScript.
# 🎨 Modern CSS Gradient Picker

A simple yet powerful web tool to create, preview, and copy CSS linear gradients in real-time. This project helps developers quickly visualize background gradients and get the exact CSS code for their projects.

## ✨ Features
* **Live Preview:** Watch the background change instantly as you pick colors.
* **Full Body Update:** The entire page background reacts to your color choices.
* **One-Click Copy:** Copies the exact `linear-gradient` CSS syntax to your clipboard.
* **Reset Option:** Quickly revert back to the default red-to-blue gradient.
* **Responsive Design:** Styled with **Tailwind CSS** for a clean, mobile-friendly UI.

## 🚀 How to Use
1. Use the color inputs to select your primary and secondary colors.
2. The preview box and page background will update automatically.
3. Click **"Copy Colors"** to get the valid CSS code: `linear-gradient(to right, #hex1, #hex2)`.
4. Click **"Reset"** to start over.
